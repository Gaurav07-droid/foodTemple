extends baseEmail

block content
    p Hi #{firstName},
    p Forgot password?Submit a patch request with your new password and confirm Password to #{url}.
    p (website not yet implemented fo this action)
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
        tbody
            tr
                td(align='left')
                    table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                        tbody
                            tr
                                td
                                    a(href=`${url}`, target='_blank') Reset password
    p if not then please ignore this email!


